{
  "name": "gmail.send",
  "version": "0.0.1",
  "risk": "high",
  "capabilities": ["gmail.send"],
  "inputs": {
    "type": "object",
    "properties": {
      "to": { "type": "string" },
      "subject": { "type": "string" },
      "body": { "type": "string" }
    },
    "required": ["to", "subject", "body"]
  },
  "outputs": {
    "type": "object",
    "properties": {
      "messageId": { "type": "string" }
    }
  }
}
